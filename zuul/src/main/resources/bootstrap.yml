spring:
  application:
    name: zuul
  cloud:
    config:
      discovery: 
        enabled: true
        serviceId: config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8084/eureka/
