spring:
  application:
    name: business2

  cloud:
    config:
      uri: http://config:8082
      label: master
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8084/eureka/
