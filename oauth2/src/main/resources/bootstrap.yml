spring:
  application:
    name: oauth2

  cloud:
    config:
      discovery: 
        enabled: true
        serviceId: config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-service:8084/eureka/

  session:
    store-type: redis
