spring:
  application:
    name: oauth2

  cloud:
    config:
      uri: http://config:8082
      label: master
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8084/eureka/

  session:
    store-type: redis
